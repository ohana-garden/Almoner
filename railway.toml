[build]
builder = "NIXPACKS"
watchPatterns = ["src/**", "package.json", "tsconfig.json", "railway.toml"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
